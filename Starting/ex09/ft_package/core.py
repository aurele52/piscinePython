def count_in_list(lst, item):
    """
    Counts the occurrences of an item in a list.
    
    :param lst: List of elements to search within.
    :param item: Item to count in the list.
    :return: Count of item in the list.
    """
    return lst.count(item)

